# إنشاء APK تلقائيًا عبر GitHub

## 1. إنشاء مستودع

1. افتح GitHub وسجّل الدخول.
2. اختر **New repository**.
3. اكتب اسمًا مثل: `yousef-sandy-app`.
4. اجعله **Private** لحماية الصور.
5. لا تضف README أو ملفات أخرى عند الإنشاء.

## 2. رفع المشروع

فك ضغط الملف، ثم ارفع **محتويات المجلد نفسه** إلى المستودع، بما فيها المجلد المخفي:

```text
.github/workflows/
```

يجب أن يظهر في الصفحة الرئيسية للمستودع:

```text
.github
assets
lib
pubspec.yaml
```

## 3. تشغيل البناء

1. افتح تبويب **Actions**.
2. اختر **Build Android APK**.
3. اضغط **Run workflow** ثم **Run workflow** مرة أخرى.
4. انتظر حتى تظهر علامة خضراء.
5. افتح عملية البناء.
6. في قسم **Artifacts** حمّل:

```text
Yousef-Sandy-APK
```

7. فك ضغط الملف، وستجد:

```text
app-release.apk
```

## نسخة اختبار أسرع

من تبويب Actions اختر **Build Test APK**، ثم حمّل:

```text
Yousef-Sandy-Test-APK
```

## تثبيت التطبيق

انقل APK إلى هاتف Android وافتحه. قد يطلب الهاتف السماح بالتثبيت من هذا المصدر.

## مهم

هذه النسخة الحالية تعرض الواجهة والميزات التجريبية محليًا. الدردشة والمايك والفيديو بين هاتفين تحتاج ربط Firebase وLiveKit في المرحلة التالية.
